#include <iomanip>
#include <iostream>
#include <string>

using namespace std;

int main()
{
	int n;
	int ne = 0;
	int m;

	cin >> n;
	cin >> m;

	int *na = new int[n];

	for (int i = 0; i < n ; i++)
	{
		<< ne;

		
		else
		{
			na[i] = i + 1 + m - 1;
			string nas = to_string(na[i]);
			cout << setw(nas.length() + 1) << setfill('+') << left << na[i];
			ne = ne + na[i];
			
		}
		

		
		
		
	}
}

